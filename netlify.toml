[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: any route falls back to index.html so client-side
# routing / direct links to hash sections work correctly on Netlify.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
